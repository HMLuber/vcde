---
title: "Startseite"
engine: markdown
---

```{=html}
<div class="page">

<!-- ══════════════════════════════════════════════════════════════
     HERO
══════════════════════════════════════════════════════════════════ -->
<div class="hero fade-up">
  <div class="hero-eyebrow">Visual Computing · Präsentation</div>
  <h1>Echtzeit-Objekterkennung<br>in Videostreams</h1>
  <p class="hero-sub">
    Wie Computer in Echtzeit Objekte in bewegten Bildern erkennen und lokalisieren –
    von den Grundlagen bis zur Live-Demo direkt im Browser.
  </p>
  <div class="hero-authors">
    <div class="author-label">Autoren</div>
    <div class="author-list">
      <span class="author-chip">Jasmin Kohlpaintner</span>
      <span class="author-chip">Henrik Berg</span>
      <span class="author-chip">Markus Luber</span>
    </div>
  </div>
  <div class="hero-tags">
    <span class="tag tag-red">YOLO</span>
    <span class="tag tag-blue">Deep Learning</span>
    <span class="tag tag-green">OpenCV</span>
    <span class="tag tag-amber">Echtzeit</span>
  </div>
</div>

<!-- ══════════════════════════════════════════════════════════════
     WAS IST ECHTZEIT-OBJEKTERKENNUNG?
══════════════════════════════════════════════════════════════════ -->
<div class="fade-up fade-up-1">
  <div class="section-label">Kurz erklärt</div>
  <div class="section-title">Was ist Echtzeit-Objekterkennung?</div>
  <p class="section-body">
    Objekterkennung ist die Fähigkeit eines Computersystems, in einem Bild oder Video
    <strong>gleichzeitig zu lokalisieren</strong> (wo befindet sich ein Objekt?) und
    <strong>zu klassifizieren</strong> (was ist es?). Das Ergebnis sind sogenannte
    <em>Bounding Boxes</em> – Rechtecke, die jedes erkannte Objekt umrahmen – zusammen mit
    einem Klassenlabel und einem Konfidenzwert.
  </p>
  <p class="section-body">
    Der Zusatz <em>Echtzeit</em> bedeutet, dass dieser Prozess schnell genug sein muss,
    um jeden Frame eines Videostreams zu verarbeiten – typischerweise 25–30 Frames pro Sekunde.
    Das stellt hohe Anforderungen an Algorithmen und Hardware und war lange Zeit
    eine der größten Herausforderungen im Bereich Computer Vision.
  </p>
</div>

<div class="card-grid fade-up fade-up-1">
  <div class="card">
    <div class="card-icon red">📦</div>
    <h3>Lokalisierung</h3>
    <p>Das System bestimmt die genaue Position jedes Objekts im Bild – definiert durch
    Koordinaten (x, y) sowie Breite und Höhe der Bounding Box.</p>
  </div>
  <div class="card">
    <div class="card-icon blue">🏷️</div>
    <h3>Klassifikation</h3>
    <p>Gleichzeitig wird jedem erkannten Objekt ein Klassenlabel zugeordnet –
    z.&nbsp;B. „Person", „Auto" oder „Hund" – mit einem zugehörigen Konfidenzwert.</p>
  </div>
  <div class="card">
    <div class="card-icon green">⚡</div>
    <h3>Echtzeitfähigkeit</h3>
    <p>Moderne One-Stage-Detektoren wie YOLO erreichen &gt;30 FPS auf Consumer-Hardware
    und ermöglichen damit den Einsatz in autonomen Fahrzeugen, Überwachungssystemen und mehr.</p>
  </div>
  <div class="card">
    <div class="card-icon amber">🌐</div>
    <h3>Im Browser</h3>
    <p>Diese Demo nutzt ONNX Runtime WebAssembly und läuft vollständig client-seitig –
    kein Server, kein Upload, Privatsphäre bleibt gewahrt.</p>
  </div>
</div>

<div class="callout info fade-up fade-up-2">
  <span class="ci">💡</span>
  <div class="ct">
    <strong>So navigierst du durch die Präsentation</strong>
    Über die Navigation oben erreichst du die einzelnen Abschnitte:
    <strong>Grundlagen</strong> erklärt Geschichte und Begriffe,
    <strong>Motivation</strong> zeigt warum Echtzeit wichtig ist,
    <strong>Technik</strong> erklärt wie YOLO funktioniert,
    und im <strong>Demo-Bereich</strong> findest du die vollständige interaktive Demo mit Vergleichstabelle.
  </div>
</div>

<hr class="divider">

<!-- ══════════════════════════════════════════════════════════════
     LIVE-DEMO
══════════════════════════════════════════════════════════════════ -->
<div class="fade-up fade-up-2">
  <div class="section-label">Interaktiv</div>
  <div class="section-title">Kurze Live-Demo</div>
  <p class="section-body">
    Lade eine Videodatei hoch und beobachte, wie YOLOv8 Objekte in Echtzeit erkennt.
    Das Modell läuft vollständig im Browser – keine Serververbindung nötig.
  </p>
</div>

<div class="demo-wrap fade-up fade-up-2">
  <div id="drop">
    <span class="drop-icon">🎬</span>
    Videodatei hier ablegen oder klicken zum Auswählen
    <input type="file" id="fi" accept="video/*" style="display:none">
    <span class="drop-hint">MP4 · WebM · MOV · AVI</span>
  </div>

  <div id="bar">
    <span id="st">Modell wird geladen …</span>
    <button id="reload" style="display:none">↩ Neue Datei</button>
    <label>Konfidenz ≥</label>
    <input type="range" id="conf" min="0.10" max="0.90" step="0.05" value="0.30">
    <span class="val" id="confv">0.30</span>
  </div>

  <div id="wrap">
    <video id="vid" loop muted playsinline></video>
    <canvas id="cv"></canvas>
  </div>
</div>

<!-- ══════════════════════════════════════════════════════════════
     FOOTER
══════════════════════════════════════════════════════════════════ -->
<div class="page-footer">
  <div class="pf-info">
    <strong>Echtzeit-Objekterkennung in Videostreams</strong><br>
    Jasmin Kohlpaintner · Henrik Berg · Markus Luber
  </div>
  <div class="pf-info">
    YOLOv8n · ONNX Runtime Web · Visual Computing
  </div>
</div>

</div>

<script>
(function () {

const CLASSES = [
  'person','bicycle','car','motorcycle','airplane','bus','train','truck',
  'boat','traffic light','fire hydrant','stop sign','parking meter','bench',
  'bird','cat','dog','horse','sheep','cow','elephant','bear','zebra','giraffe',
  'backpack','umbrella','handbag','tie','suitcase','frisbee','skis','snowboard',
  'sports ball','kite','baseball bat','baseball glove','skateboard','surfboard',
  'tennis racket','bottle','wine glass','cup','fork','knife','spoon','bowl',
  'banana','apple','sandwich','orange','broccoli','carrot','hot dog','pizza',
  'donut','cake','chair','couch','potted plant','bed','dining table','toilet',
  'tv','laptop','mouse','remote','keyboard','cell phone','microwave','oven',
  'toaster','sink','refrigerator','book','clock','vase','scissors',
  'teddy bear','hair drier','toothbrush',
];

function hsl(name) {
  let h = 0;
  for (let i = 0; i < name.length; i++) h = (h * 31 + name.charCodeAt(i)) & 0xffff;
  return `hsl(${h % 360},65%,48%)`;
}

const drop   = document.getElementById('drop');
const fi     = document.getElementById('fi');
const wrap   = document.getElementById('wrap');
const vid    = document.getElementById('vid');
const cv     = document.getElementById('cv');
const ctx    = cv.getContext('2d');
const stEl   = document.getElementById('st');
const confEl = document.getElementById('conf');
const confv  = document.getElementById('confv');
const reload = document.getElementById('reload');

confEl.addEventListener('input', () => confv.textContent = parseFloat(confEl.value).toFixed(2));

const worker = new Worker('worker.js');
let ready = false, sz = 640, provider = 'WASM';
let lastDets = [], running = false;
let resolveResult = null;

worker.onmessage = ({ data }) => {
  if (data.type === 'ready') {
    ready    = true;
    sz       = data.inputSize;
    provider = data.provider;
    stEl.textContent = `✓ Modell geladen · ${provider}`;
  } else if (data.type === 'result') {
    lastDets = data.detections;
    if (resolveResult) { resolveResult(); resolveResult = null; }
  } else if (data.type === 'error') {
    stEl.textContent = `⚠ Fehler: ${data.message}`;
  }
};

drop.addEventListener('click', () => fi.click());
drop.addEventListener('dragover',  e => { e.preventDefault(); drop.classList.add('over'); });
drop.addEventListener('dragleave', () => drop.classList.remove('over'));
drop.addEventListener('drop', e => {
  e.preventDefault(); drop.classList.remove('over');
  if (e.dataTransfer.files[0]) load(e.dataTransfer.files[0]);
});
fi.addEventListener('change', () => { if (fi.files[0]) load(fi.files[0]); });

reload.addEventListener('click', () => {
  running = false; lastDets = [];
  vid.pause(); vid.src = '';
  wrap.style.display = 'none';
  drop.style.display = '';
  reload.style.display = 'none';
  stEl.textContent = `✓ Modell geladen · ${provider}`;
});

function load(file) {
  vid.src = URL.createObjectURL(file);
  vid.onloadeddata = () => {
    drop.style.display = 'none';
    reload.style.display = '';
    wrap.style.display  = 'block';
    vid.play();
    if (!running) { running = true; inferLoop(); drawLoop(); }
  };
}

const off  = document.createElement('canvas');
const octx = off.getContext('2d', { willReadFrequently: true });

function prep(src, W, H) {
  const scale = Math.min(sz / W, sz / H);
  const nw = Math.round(W * scale), nh = Math.round(H * scale);
  const px = Math.floor((sz - nw) / 2), py = Math.floor((sz - nh) / 2);
  off.width = sz; off.height = sz;
  octx.fillStyle = 'rgb(114,114,114)';
  octx.fillRect(0, 0, sz, sz);
  octx.drawImage(src, px, py, nw, nh);
  const pix = octx.getImageData(0, 0, sz, sz).data;
  const f32 = new Float32Array(3 * sz * sz);
  for (let i = 0; i < sz * sz; i++) {
    f32[i]               = pix[i * 4]     / 255;
    f32[sz * sz + i]     = pix[i * 4 + 1] / 255;
    f32[2 * sz * sz + i] = pix[i * 4 + 2] / 255;
  }
  return { f32, scale, padX: px, padY: py };
}

async function inferLoop() {
  while (running) {
    if (!ready || vid.readyState < 2 || vid.paused) {
      await new Promise(r => setTimeout(r, 50)); continue;
    }
    const W = vid.videoWidth, H = vid.videoHeight;
    if (!W) { await new Promise(r => setTimeout(r, 16)); continue; }
    const t0 = performance.now();
    const { f32, scale, padX, padY } = prep(vid, W, H);
    await new Promise(r => {
      resolveResult = r;
      worker.postMessage({
        type: 'infer', float32: f32,
        scale, padX, padY, origW: W, origH: H,
        confThresh: parseFloat(confEl.value),
      }, [f32.buffer]);
    });
    const fps = (1000 / (performance.now() - t0)).toFixed(1);
    stEl.textContent = `${provider} · ${fps} FPS · ${lastDets.length} Objekt(e) erkannt`;
  }
}

function drawLoop() {
  if (!running) return;
  const W = vid.videoWidth, H = vid.videoHeight;
  if (W > 0 && H > 0) {
    if (cv.width !== W || cv.height !== H) { cv.width = W; cv.height = H; }
    ctx.clearRect(0, 0, W, H);
    for (const d of lastDets) {
      const c = hsl(d.class);
      const [x, y, w, h] = d.bbox;
      ctx.strokeStyle = c; ctx.lineWidth = 2.5;
      ctx.strokeRect(x, y, w, h);
      const lbl = `${d.class} ${Math.round(d.score * 100)}%`;
      ctx.font = 'bold 24px DM Sans, system-ui, sans-serif';
      const tw = ctx.measureText(lbl).width;
      ctx.fillStyle = c;
      ctx.fillRect(x, y - 30, tw + 12, 30);
      ctx.fillStyle = '#fff';
      ctx.fillText(lbl, x + 6, y - 7);
    }
  }
  requestAnimationFrame(drawLoop);
}

})();
</script>
```
